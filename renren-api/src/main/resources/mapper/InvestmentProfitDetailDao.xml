<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.dao.InvestmentProfitDetailDao">

	<resultMap type="io.renren.entity.InvestmentProfitDetailEntity" id="investmentProfitDetailMap">
		<result property="id" column="id"/>
		<result property="investmentId" column="investment_id"/>
		<result property="userId" column="user_id"/>
		<result property="projectId" column="project_id"/>
		<result property="profitType" column="profit_type"/>
		<result property="profitAmount" column="profit_amount"/>
		<result property="profitDate" column="profit_date"/>
		<result property="status" column="status"/>
		<result property="remark" column="remark"/>
		<result property="createDate" column="create_date"/>
		<result property="updateDate" column="update_date"/>
	</resultMap>

	<!-- 根据投资记录ID查询收益明细 -->
	<select id="selectByInvestmentId" resultMap="investmentProfitDetailMap">
		SELECT * FROM tb_investment_profit_detail 
		WHERE investment_id = #{investmentId}
		ORDER BY profit_date DESC
	</select>

	<!-- 根据用户ID查询收益明细 -->
	<select id="selectByUserId" resultMap="investmentProfitDetailMap">
		SELECT * FROM tb_investment_profit_detail 
		WHERE user_id = #{userId}
		ORDER BY profit_date DESC
	</select>

	<!-- 根据项目ID查询收益明细 -->
	<select id="selectByProjectId" resultMap="investmentProfitDetailMap">
		SELECT * FROM tb_investment_profit_detail 
		WHERE project_id = #{projectId}
		ORDER BY profit_date DESC
	</select>

	<!-- 根据收益类型查询收益明细 -->
	<select id="selectByProfitType" resultMap="investmentProfitDetailMap">
		SELECT * FROM tb_investment_profit_detail 
		WHERE profit_type = #{profitType}
		ORDER BY profit_date DESC
	</select>

	<!-- 查询用户总收益金额 -->
	<select id="selectTotalProfitByUserId" resultType="long">
		SELECT COALESCE(SUM(profit_amount), 0) 
		FROM tb_investment_profit_detail 
		WHERE user_id = #{userId} AND status = 1
	</select>

	<!-- 查询用户指定类型的收益金额 -->
	<select id="selectProfitByUserIdAndType" resultType="long">
		SELECT COALESCE(SUM(profit_amount), 0) 
		FROM tb_investment_profit_detail 
		WHERE user_id = #{userId} AND profit_type = #{profitType} AND status = 1
	</select>

	<!-- 分页查询用户收益明细 -->
	<select id="selectPageByUserId" resultMap="investmentProfitDetailMap">
		SELECT * FROM tb_investment_profit_detail 
		WHERE user_id = #{userId}
		<if test="profitType != null">
			AND profit_type = #{profitType}
		</if>
		<if test="status != null">
			AND status = #{status}
		</if>
		ORDER BY profit_date DESC
		LIMIT #{offset}, #{limit}
	</select>

	<!-- 统计用户收益明细总数 -->
	<select id="selectCountByUserId" resultType="int">
		SELECT COUNT(*) FROM tb_investment_profit_detail 
		WHERE user_id = #{userId}
		<if test="profitType != null">
			AND profit_type = #{profitType}
		</if>
		<if test="status != null">
			AND status = #{status}
		</if>
	</select>

</mapper>
