# 支付服务扩展说明

## 概述
本文档说明了支付服务的扩展架构，通过工厂模式和接口设计，使支付服务易于扩展和维护。

## 架构设计

### 1. 核心接口
- **PaymentService**: 定义统一的支付服务接口
- **PaymentServiceFactory**: 支付服务工厂，负责管理和创建支付服务实例

### 2. 现有实现
- **QePayPaymentService**: QePay支付服务实现
- **WePayPaymentServiceAdapter**: WePay支付服务适配器

## 如何添加新的支付服务

### 步骤1：实现PaymentService接口

```java
@Service
public class NewPaymentService implements PaymentService {
    
    @Override
    public String getServiceName() {
        return "NewPay";
    }
    
    @Override
    public boolean supports(String merchantName) {
        // 定义支持的商户名称
        return "NewMerchant".equals(merchantName);
    }
    
    @Override
    public PaymentResponseDTO createPaymentOrder(UserEntity user, Long amount, String orderNo, 
                                               PayChannelEntity payChannel, PayMerchantEntity payMerchant) {
        // 实现具体的支付逻辑
        // ...
        return paymentResponse;
    }
}
```

### 步骤2：配置商户名称
在支付商户配置中，将商户名称设置为新支付服务支持的名称。

### 步骤3：自动发现
支付服务工厂会自动发现并注册新的支付服务，无需修改其他代码。

## 架构优势

### 1. 易于扩展
- 添加新支付服务只需实现接口
- 无需修改现有代码
- 自动服务发现

### 2. 统一管理
- 所有支付服务通过工厂统一管理
- 统一的调用入口
- 统一的错误处理

### 3. 松耦合
- 支付服务之间相互独立
- 可以独立测试和部署
- 易于维护和升级

## 使用示例

### 在ChargeOrderServiceImpl中使用

```java
@Autowired
private PaymentServiceFactory paymentServiceFactory;

// 创建支付订单
PaymentResponseDTO paymentResponse = paymentServiceFactory.createPaymentOrder(
    user, amount / 100, orderno, payChannelEntity, payMerchantEntity);
```

### 获取支持的支付服务

```java
String[] supportedServices = paymentServiceFactory.getSupportedServices();
```

## 配置说明

### 商户名称映射
- **Wepay2886** → WePayPaymentServiceAdapter
- **其他商户** → QePayPaymentService

### 添加新映射
只需在新支付服务的`supports()`方法中定义支持的商户名称即可。

## 注意事项

1. **商户名称唯一性**: 确保每个商户名称只被一个支付服务支持
2. **错误处理**: 实现支付服务时要注意异常处理
3. **日志记录**: 添加适当的日志记录便于调试
4. **测试覆盖**: 为新支付服务编写单元测试

## 示例代码

参考`ExamplePaymentService.java`了解完整的实现示例。
