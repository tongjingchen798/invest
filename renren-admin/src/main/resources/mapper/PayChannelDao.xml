<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.paychannel.dao.PayChannelDao">

    <resultMap type="io.renren.modules.paychannel.entity.PayChannelEntity" id="payChannelMap">
        <result property="channelid" column="channelid"/>
        <result property="channelName" column="channel_name"/>
        <result property="channelType" column="channel_type"/>
        <result property="merchantid" column="merchantid"/>
        <result property="merchantname" column="merchantname"/>
        <result property="status" column="status"/>
        <result property="usdtGiftRatio" column="usdt_gift_ratio"/>
        <result property="usdtLocalCurrencyRate" column="usdt_local_currency_rate"/>
        <result property="chargeorwithdraw" column="chargeorwithdraw"/>
        <result property="createDate" column="create_date"/>
        <result property="updateDate" column="update_date"/>
    </resultMap>

    <!-- 分页查询 -->
    <select id="selectPage" resultMap="payChannelMap">
        SELECT * FROM tb_pay_channel
        <where>
            <if test="channelName != null and channelName != ''">
                AND channel_name LIKE CONCAT('%', #{channelName}, '%')
            </if>
            <if test="channelType != null and channelType != ''">
                AND channel_type = #{channelType}
            </if>
            <if test="merchantname != null and merchantname != ''">
                AND merchantname LIKE CONCAT('%', #{merchantname}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="chargeorwithdraw != null and chargeorwithdraw != ''">
                AND chargeorwithdraw = #{chargeorwithdraw}
            </if>
        </where>
        ORDER BY create_date DESC
    </select>

    <!-- 统计总数 -->
    <select id="selectCount" resultType="int">
        SELECT COUNT(*) FROM tb_pay_channel
        <where>
            <if test="channelName != null and channelName != ''">
                AND channel_name LIKE CONCAT('%', #{channelName}, '%')
            </if>
            <if test="channelType != null and channelType != ''">
                AND channel_type = #{channelType}
            </if>
            <if test="merchantname != null and merchantname != ''">
                AND merchantname LIKE CONCAT('%', #{merchantname}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="chargeorwithdraw != null and chargeorwithdraw != ''">
                AND chargeorwithdraw = #{chargeorwithdraw}
            </if>
        </where>
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="payChannelMap">
        SELECT * FROM tb_pay_channel WHERE channelid = #{id}
    </select>

    <!-- 查询列表 -->
    <select id="selectList" resultMap="payChannelMap">
        SELECT * FROM tb_pay_channel
        <where>
            <if test="channelName != null and channelName != ''">
                AND channel_name LIKE CONCAT('%', #{channelName}, '%')
            </if>
            <if test="channelType != null and channelType != ''">
                AND channel_type = #{channelType}
            </if>
            <if test="merchantname != null and merchantname != ''">
                AND merchantname LIKE CONCAT('%', #{merchantname}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="chargeorwithdraw != null and chargeorwithdraw != ''">
                AND chargeorwithdraw = #{chargeorwithdraw}
            </if>
        </where>
        ORDER BY create_date DESC
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="io.renren.modules.paychannel.entity.PayChannelEntity" useGeneratedKeys="true" keyProperty="channelid">
        INSERT INTO tb_pay_channel (
            channel_name, channel_type, merchantid, merchantname, status,
            usdt_gift_ratio, usdt_local_currency_rate, chargeorwithdraw,
            create_date, update_date
        ) VALUES (
            #{channelName}, #{channelType}, #{merchantid}, #{merchantname}, #{status},
            #{usdtGiftRatio}, #{usdtLocalCurrencyRate}, #{chargeorwithdraw},
            #{createDate}, #{updateDate}
        )
    </insert>

    <!-- 更新 -->
    <update id="updateById" parameterType="io.renren.modules.paychannel.entity.PayChannelEntity">
        UPDATE tb_pay_channel SET
            channel_name = #{channelName},
            channel_type = #{channelType},
            merchantid = #{merchantid},
            merchantname = #{merchantname},
            status = #{status},
            usdt_gift_ratio = #{usdtGiftRatio},
            usdt_local_currency_rate = #{usdtLocalCurrencyRate},
            chargeorwithdraw = #{chargeorwithdraw},
            update_date = #{updateDate}
        WHERE channelid = #{channelid}
    </update>

    <!-- 删除 -->
    <delete id="deleteById">
        DELETE FROM tb_pay_channel WHERE channelid = #{id}
    </delete>

    <!-- 批量删除 -->
    <delete id="deleteBatchIds">
        DELETE FROM tb_pay_channel WHERE channelid IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
