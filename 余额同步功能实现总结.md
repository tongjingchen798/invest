# 每日收益累加到可用余额功能实现总结

## 📋 需求概述

用户希望每日获取的项目盈利金额能够累加到用户表的可用余额字段（`assets`），方便用户继续投资。

**重要说明：**
- **`assets`**：用户的实际可用余额（钱包余额），用于投资等操作
- **`cashwithdrawable`**：用户的可提现额度，这是一个额度概念，不是实际的钱包余额
- 两个字段是独立的，有不同的业务含义

## 🔧 技术实现

### 1. 核心修改

#### 1.1 修改每日收益计算逻辑
**文件：** `renren-api/src/main/java/io/renren/schedule/UserInvestmentProfitSchedule.java`

**修改内容：**
```java
private void updateUserBalance(Long userId, BigDecimal profitAmount) {
    Long profitAmountInCents = profitAmount.longValue();
    
    // 1. 更新用户可用余额（assets字段）
    int assetsResult = userDao.addUserBalance(userId, profitAmountInCents);
    
    // 2. 更新可提现额度和收益统计字段
    int profitResult = userDao.updateAllCashProfitFields(userId, profitAmountInCents);
}
```

**实现效果：**
- 每日收益同时累加到 `assets`（可用余额）和 `cashwithdrawable`（可提现额度）
- 用户可以使用 `assets` 中的收益继续投资
- 用户可以使用 `cashwithdrawable` 中的额度进行提现

### 2. 业务逻辑保持不变

#### 2.1 提现操作
**文件：** `renren-api/src/main/java/io/renren/service/impl/WithdrawServiceImpl.java`

**保持原有逻辑：**
```java
// 只扣减可提现额度，不扣减可用余额
user.setCashwithdrawable(oldCashWithdrawable - requestDTO.getAmount());
user.setFreezeBalance(oldFreezeBalance + requestDTO.getAmount());
```

**原因：**
- 提现是使用可提现额度，不是使用可用余额
- 两个字段有不同的业务含义

#### 2.2 余额查询
**文件：** `renren-api/src/main/java/io/renren/controller/ApiUserController.java`

**保持原有逻辑：**
```java
// 总余额 = 可用余额 + 可提现额度 + 待收利息
Long balance = balanceDTO.getAssets() + user.getCashwithdrawable() + dsAmount;
```

**原因：**
- 用户的总资产包括可用余额和可提现额度
- 两个字段需要分别显示

## 📊 业务流程图更新

**文件：** `用户业务流程图.md`

**更新内容：**
- 更新了投资收益流程图，显示分别更新两个字段
- 更新了提现流程图，说明只扣减可提现额度
- 新增了字段说明和业务逻辑说明

## ✅ 实现效果

### 1. 每日收益计算
- ✅ 每日项目盈利金额同时累加到 `assets`（可用余额）和 `cashwithdrawable`（可提现额度）字段
- ✅ 用户可以使用 `assets` 中的收益继续投资
- ✅ 用户可以使用 `cashwithdrawable` 中的额度进行提现
- ✅ 保持了原有的收益统计功能

### 2. 提现操作
- ✅ 提现时只扣减 `cashwithdrawable`（可提现额度）字段
- ✅ 不扣减 `assets`（可用余额）字段，符合业务逻辑
- ✅ 提现失败时只恢复 `cashwithdrawable` 字段

### 3. 数据独立性
- ✅ 两个字段保持独立，符合业务逻辑
- ✅ 不需要数据同步，因为字段含义不同
- ✅ 业务逻辑清晰，易于维护

### 4. 用户体验
- ✅ 用户可以看到正确的可用余额和可提现额度
- ✅ 用户可以使用收益继续投资
- ✅ 用户可以使用额度进行提现

## 🔍 技术特点

1. **业务逻辑清晰**：两个字段有明确的业务含义
2. **向后兼容**：保持了原有的业务逻辑和接口
3. **简单实现**：只需要修改每日收益计算逻辑
4. **易于维护**：不需要复杂的数据同步机制
5. **错误处理**：完善的异常处理机制

## 📝 注意事项

1. **字段含义**：理解 `assets` 和 `cashwithdrawable` 的不同含义
2. **业务逻辑**：提现使用可提现额度，投资使用可用余额
3. **数据一致性**：两个字段不需要保持同步，因为含义不同
4. **测试验证**：建议在测试环境充分测试后再部署到生产环境

## 🚀 部署建议

1. 先在测试环境部署和测试
2. 验证每日收益是否正确累加到两个字段
3. 验证用户可以使用收益继续投资
4. 验证提现逻辑正常工作
5. 部署到生产环境

## 📞 技术支持

如有问题，请检查：
1. 每日收益是否正确累加到 `assets` 字段
2. 用户是否可以使用收益继续投资
3. 提现逻辑是否正常工作
4. 余额显示是否正确
